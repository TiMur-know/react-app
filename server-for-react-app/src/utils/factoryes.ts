/* eslint-disable prettier/prettier */
/*import { Factory } from 'typeorm-seeding';
import { Log } from "../entity/Log.entity";
import { Task } from "../entity/Task.entity";
import { TaskList } from "../entity/TaskList.entity"


// Define factories for your Entities
const taskListFactory: Factory<TaskList> = (factory) => factory(TaskList)({
  name: factory.faker.lorem.sentence(),
});

const taskFactory: Factory<Task> = (factory) => factory(Task)({
  name: factory.faker.lorem.sentence(),
  description: factory.faker.lorem.paragraph(),
  dueDate: factory.faker.date.future(),
  priority: factory.random.arrayElement(['High', 'Medium', 'Low']),
});

const logFactory: Factory<Log> = (factory) => factory(Log)({
  message: factory.faker.lorem.sentence(),
  timestamp: factory.faker.date.recent(),
  primaryWords: factory.random.words(3),
  secondaryWords: factory.random.words(5),
});

export default { taskListFactory, taskFactory, logFactory };*/